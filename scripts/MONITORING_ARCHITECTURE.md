/**
 * TK Auto Reply 脚本的监听机制分析
 */

console.log(`
========================================================================================
TK Auto Reply 脚本监听机制
========================================================================================

✅ 1️⃣  Fetch API 监听 (hookFetchAndXHR)
   ├─ 监听: /v1/message/ack
   ├─ 监听: /v1/message/get_by_user_combo  
   ├─ 监听: /message, /conversation, /im, /chat (旧接口)
   └─ 解析: Protobuf 二进制数据

✅ 2️⃣  XMLHttpRequest 监听 (hookFetchAndXHR)
   ├─ 监听: /v1/message/ack
   ├─ 监听: /v1/message/get_by_user_combo
   ├─ 监听: /message, /conversation, /im, /chat (旧接口)
   └─ 解析: Protobuf 二进制数据

✅ 3️⃣  WebSocket 监听 (hookWebSocket)
   ├─ 拦截: window.WebSocket 构造函数
   ├─ 从你提供的十六进制数据来看：
   │   这些数据可能不是来自 WebSocket
   │   而是来自 HTTP API (fetch/XHR)
   └─ 解析: Protobuf + 关键词检测

========================================================================================
根据你提供的数据分析：
========================================================================================

你提供的十六进制: 08c205109a4e180022024f4b...
├─ 解析结果: ✅ 成功
├─ 字段数: 10
├─ 字符串: ["OK", "20260217191858...", "Singapore-Central"]
└─ 结论: 这是 Heartbeat/Status 消息，不是聊天内容

脚本会拦截这个数据，因为：
1. ✓ 来自 /v1/message/ack 或 /v1/message/get_by_user_combo
2. ✓ Protobuf 二进制格式
3. ✓ 被脚本成功解析

脚本不打印"成功提取消息"，因为：
1. ✗ 字符串都是系统字段（"OK"、"Singapore-Central"）
2. ✗ ID 是纯十六进制，被过滤了
3. ✗ 没有找到有效的聊天文本

========================================================================================
现在的日志输出应该包含：
========================================================================================

[TK-Auto-Reply] ⏳ TK Auto Reply 脚本正在加载...
[TK-Auto-Reply] 📡 [关键接口] 检测到: ack 字节数: 91
[TK-Auto-Reply]   ✓ Protobuf 解析成功，字段数: 10
[TK-Auto-Reply]   ✓ 提取到字符串: OK,20260217191858F28333C5AC9FFDEE4E1E,Singapore-Central
[TK-Auto-Reply]   ℹ️  未找到有效消息内容（可能是 heartbeat 消息）

========================================================================================
要看到"成功提取消息"，需要：
========================================================================================

1. 真实的用户聊天消息（不是 heartbeat）
   - 包含用户的聊天文本
   - 不是纯粹的状态/心跳消息

2. 或者调整脚本的过滤条件
   - 修改 extractMessageFromProtobuf() 中的过滤逻辑
   - 允许更短的字符串
   - 允许包含特殊字符的消息

3. 监听更多接口
   - 可能还有其他 TikTok API 端点用来传输聊天消息
   - 需要抓包分析真实聊天场景

========================================================================================
`)
